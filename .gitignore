node_modules/
dist/
.env
.env.local
.DS_Store
*.log

# Mobile (RN-web) build output — built by deploy.sh from ../openchat-mobile.
client-mobile/dist/

# Beads embedded dolt DB runtime files — don't commit
.beads/embeddeddolt/
.beads/backup/
.beads/.jsonl.lock
.beads/last-touched
.beads/export-state.json
