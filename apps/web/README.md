# OpenChat Legacy Web Client

**LEGACY: This codebase is frozen.**

This directory (`apps/web`) contains the legacy React + Vite client source. It is retained purely for history and migration reference and is **no longer built or served**.

The canonical, active web application is now built out of `apps/mobile` using `react-native-web`. All new development for both mobile and web surfaces must happen in `apps/mobile`.
